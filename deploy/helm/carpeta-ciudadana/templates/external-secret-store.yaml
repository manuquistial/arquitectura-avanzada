# External Secrets Operator - SecretStore para Azure Key Vault
# NOTA: ClusterSecretStore "azure-keyvault" se gestiona desde Terraform (application layer)
# Este archivo no crea el ClusterSecretStore, solo se mantiene como referencia
# Los External Secrets usan el ClusterSecretStore "azure-keyvault" creado por Terraform
