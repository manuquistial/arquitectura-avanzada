# =============================================================================
# Docker Configuration for Build and Push Script
# =============================================================================
# Configuración para el script de build y push de Docker

# Docker Hub Configuration
DOCKER_HUB_USERNAME=manuelquistial
DOCKER_HUB_REPOSITORY=carpeta-ciudadana

# Image Configuration
VERSION=latest
ENVIRONMENT=production

# Build Configuration
BUILD_NO_CACHE=true
BUILD_PULL=true

# Optional: Specific services to build (leave empty to build all)
# BUILD_SERVICES=("auth" "citizen" "frontend")

# Optional: Skip specific services
# SKIP_SERVICES=("metadata" "read_models")
