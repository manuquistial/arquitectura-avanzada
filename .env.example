# Root .env.example (used by docker-compose and CI defaults)

# Docker Hub
DOCKER_USERNAME=your_dockerhub_username
TAG=local

# Optional: Frontend runtime vars (if needed by Next.js at build time)
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000